logging:
  level:
    root: info
    org.codebusters: trace

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:agent-test
    username: gt
    password: gt
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

jwt:
  secret: N1y2rHlRiosNm9fiWgpArgj2eLxxhdIy

agent:
  genre:
    max-length: 10
    char-whitelist: []