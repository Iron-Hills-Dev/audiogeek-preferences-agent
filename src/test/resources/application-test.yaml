logging:
  level:
    root: info
    org.codebusters: trace

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:agent-test
    username: gt
    password: gt

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

jwt:
  secret: B8444269FC8F514C79FDE4631EE9EF22162211D
  algorithm: hmacSHA256